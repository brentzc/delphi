<rect
    role="button"
    x={{this.x}}
    y={{this.y}}
    height={{this.height}}
    width={{this.width}}
    class="fill-current {{this.barColor}}"
    {{on 'mouseover' this.mouseover}}
    {{on 'mouseout' this.mouseout}}
/>